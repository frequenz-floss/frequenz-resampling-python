[package]
name = "frequenz-resampling-python"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
chrono = "0.4"
frequenz-resampling = "0.3.0"
pyo3 = { version = "0.27", features = [
  "extension-module",
  "chrono",
] }
