{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Frequenz Resampling","text":""},{"location":"#introduction","title":"Introduction","text":"<p><code>frequenz-resampling</code> provides fast, typed resampling for timestamped numeric data in Python.</p> <p>The library is backed by the Rust resampler <code>frequenz-floss/frequenz-resampling-rs</code> and exposed through a small Python API. It supports incremental stream resampling through a <code>Resampler</code> object as well as one-shot resampling with the <code>resample()</code> convenience function.</p> <p>Main capabilities:</p> <ul> <li>fixed-interval resampling with timezone-aware <code>datetime</code> timestamps</li> <li>configurable interval semantics via <code>Closed</code> and <code>Label</code></li> <li>multiple aggregation functions via <code>ResamplingFunction</code>:   <code>AVERAGE</code>, <code>SUM</code>, <code>MAX</code>, <code>MIN</code>, <code>FIRST</code>, <code>LAST</code>, <code>COUNT</code>, <code>COALESCE</code></li> <li>typed Python API (<code>py.typed</code>) and generated API docs</li> </ul>"},{"location":"#supported-platforms","title":"Supported Platforms","text":"<p>The following platforms are officially supported (tested):</p> <ul> <li>Python: 3.11, 3.12, 3.13, 3.14</li> <li>Operating System: Ubuntu Linux 24.04, Windows, macOS</li> <li>Architectures: amd64, arm64</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#install","title":"Install","text":"<pre><code>python -m pip install frequenz-resampling\n</code></pre>"},{"location":"#stream-resampling-with-resampler","title":"Stream resampling with <code>Resampler</code>","text":"<pre><code>import datetime as dt\n\nfrom frequenz.resampling import Closed, Label, Resampler, ResamplingFunction\n\nstart = dt.datetime(1970, 1, 1, tzinfo=dt.timezone.utc)\nstep = dt.timedelta(seconds=1)\n\nresampler = Resampler(\n    dt.timedelta(seconds=5),\n    ResamplingFunction.AVERAGE,\n    max_age_in_intervals=1,\n    start=start,\n    closed=Closed.LEFT,\n    label=Label.RIGHT,\n)\n\nfor i in range(10):\n    resampler.push_sample(timestamp=start + i * step, value=float(i + 1))\n\nresult = resampler.resample(start + 10 * step)\nprint(result)\n# [(1970-01-01 00:00:05+00:00, 3.0), (1970-01-01 00:00:10+00:00, 8.0)]\n</code></pre>"},{"location":"#one-shot-resampling-with-resample","title":"One-shot resampling with <code>resample()</code>","text":"<pre><code>import datetime as dt\n\nfrom frequenz.resampling import Closed, Label, ResamplingFunction, resample\n\nstart = dt.datetime(1970, 1, 1, tzinfo=dt.timezone.utc)\nstep = dt.timedelta(seconds=1)\ndata = [(start + i * step, float(i + 1)) for i in range(10)]\n\nresult = resample(\n    data,\n    dt.timedelta(seconds=5),\n    ResamplingFunction.SUM,\n    closed=Closed.LEFT,\n    label=Label.RIGHT,\n)\nprint(result)\n</code></pre>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li>User and API documentation:   https://frequenz-floss.github.io/frequenz-resampling-python/</li> <li>Changelog / releases:   https://github.com/frequenz-floss/frequenz-resampling-python/releases</li> </ul>"},{"location":"#contributing","title":"Contributing","text":"<p>Contributions are welcome. For development setup, testing, docs, and release workflow, see the Contributing Guide.</p>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the MIT License.</p>"},{"location":"CONTRIBUTING/","title":"Contributing to Frequenz Resampling","text":""},{"location":"CONTRIBUTING/#build","title":"Build","text":"<p>You can use <code>build</code> to simply build the source and binary distribution:</p> <pre><code>python -m pip install build\npython -m build\n</code></pre>"},{"location":"CONTRIBUTING/#local-development","title":"Local development","text":"<p>You can use editable installs to develop the project locally (it will install all the dependencies too):</p> <pre><code>python -m pip install -e .\n</code></pre> <p>Or you can install all development dependencies (<code>mypy</code>, <code>pylint</code>, <code>pytest</code>, etc.) in one go too: <pre><code>python -m pip install -e .[dev]\n</code></pre></p> <p>If you don't want to install all the dependencies, you can also use <code>nox</code> to run the tests and other checks creating its own virtual environments:</p> <pre><code>python -m pip install .[dev-noxfile]\nnox\n</code></pre> <p>You can also use <code>nox -R</code> to reuse the current testing environment to speed up test at the expense of a higher chance to end up with a dirty test environment.</p>"},{"location":"CONTRIBUTING/#running-tests-checks-individually","title":"Running tests / checks individually","text":"<p>For a better development test cycle you can install the runtime and test dependencies and run <code>pytest</code> manually.</p> <pre><code>python -m pip install .[dev-pytest]  # included in .[dev] too\n\n# And for example\npytest tests/test_*.py\n</code></pre> <p>Or you can use <code>nox</code>:</p> <pre><code>nox -R -s pytest -- tests/test_*.py\n</code></pre> <p>The same appliest to <code>pylint</code> or <code>mypy</code> for example:</p> <pre><code>nox -R -s pylint -- tests/test_*.py\nnox -R -s mypy -- tests/test_*.py\n</code></pre>"},{"location":"CONTRIBUTING/#building-the-documentation","title":"Building the documentation","text":"<p>To build the documentation, first install the dependencies (if you didn't install all <code>dev</code> dependencies):</p> <pre><code>python -m pip install -e .[dev-mkdocs]\n</code></pre> <p>Then you can build the documentation (it will be written in the <code>site/</code> directory):</p> <pre><code>mkdocs build\n</code></pre> <p>Or you can just serve the documentation without building it using:</p> <pre><code>mkdocs serve\n</code></pre> <p>Your site will be updated live when you change your files (provided that you used <code>pip install -e .</code>, beware of a common pitfall of using <code>pip install</code> without <code>-e</code>, in that case the API reference won't change unless you do a new <code>pip install</code>).</p> <p>To build multi-version documentation, we use mike. If you want to see how the multi-version sites looks like locally, you can use:</p> <pre><code>mike deploy my-version\nmike set-default my-version\nmike serve\n</code></pre> <p><code>mike</code> works in mysterious ways. Some basic information:</p> <ul> <li><code>mike deploy</code> will do a <code>mike build</code> and write the results to your local <code>gh-pages</code> branch. <code>my-version</code> is an arbitrary name for the local version   you want to preview.</li> <li><code>mike set-default</code> is needed so when you serve the documentation, it goes to   your newly produced documentation by default.</li> <li><code>mike serve</code> will serve the contents of your local <code>gh-pages</code> branch. Be   aware that, unlike <code>mkdocs serve</code>, changes to the sources won't be shown   live, as the <code>mike deploy</code> step is needed to refresh them.</li> </ul> <p>Be careful not to use <code>--push</code> with <code>mike deploy</code>, otherwise it will push your local <code>gh-pages</code> branch to the <code>origin</code> remote.</p> <p>That said, if you want to test the actual website in your fork, you can always use <code>mike deploy --push --remote your-fork-remote</code>, and then access the GitHub pages produced for your fork.</p>"},{"location":"CONTRIBUTING/#releasing","title":"Releasing","text":"<p>These are the steps to create a new release:</p> <ol> <li> <p>Get the latest head you want to create a release from.</p> </li> <li> <p>Update the <code>RELEASE_NOTES.md</code> file if it is not complete, up to date, and    remove template comments (<code>&lt;!-- ... -&gt;</code>) and empty sections. Submit a pull    request if an update is needed, wait until it is merged, and update the    latest head you want to create a release from to get the new merged pull    request.</p> </li> <li> <p>Create a new signed tag using the release notes and    a semver compatible version number with a <code>v</code> prefix,    for example:</p> </li> </ol> <pre><code>git tag -s --cleanup=whitespace -F RELEASE_NOTES.md v0.0.1\n</code></pre> <ol> <li> <p>Push the new tag.</p> </li> <li> <p>A GitHub action will test the tag and if all goes well it will create    a GitHub    Release,    and upload a new package to    PyPI    automatically.</p> </li> <li> <p>Once this is done, reset the <code>RELEASE_NOTES.md</code> with the template:</p> </li> </ol> <pre><code>cp .github/RELEASE_NOTES.template.md RELEASE_NOTES.md\n</code></pre> <p>Commit the new release notes and create a PR (this step should be automated    eventually too).</p> <ol> <li>Celebrate!</li> </ol>"},{"location":"SUMMARY/","title":"SUMMARY","text":"<ul> <li>Home</li> <li>API Reference</li> <li>Contributing</li> </ul>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>resampling</li> </ul>"},{"location":"reference/resampling/","title":"resampling","text":""},{"location":"reference/resampling/#resampling","title":"resampling","text":"<p>Frequenz Resampling Python Bindings.</p>"},{"location":"reference/resampling/#resampling-classes","title":"Classes","text":""},{"location":"reference/resampling/#resampling.Closed","title":"resampling.Closed","text":"<p>               Bases: <code>Enum</code></p> <p>Controls which edge of an interval is closed for sample membership.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@unique\nclass Closed(Enum):\n    \"\"\"Controls which edge of an interval is closed for sample membership.\"\"\"\n\n    LEFT = 0\n    \"\"\"Left-closed, right-open intervals: `[start, end)`\"\"\"\n    RIGHT = 1\n    \"\"\"Left-open, right-closed intervals: `(start, end]`\"\"\"\n\n    @staticmethod\n    def values() -&gt; list[int]:\n        \"\"\"Returns a list of all values of the enum.\"\"\"\n\n    @staticmethod\n    def members() -&gt; list[tuple[str, int]]:\n        \"\"\"Returns a list of all members of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Closed-attributes","title":"Attributes","text":""},{"location":"reference/resampling/#resampling.Closed.LEFT","title":"LEFT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LEFT = 0\n</code></pre> <p>Left-closed, right-open intervals: <code>[start, end)</code></p>"},{"location":"reference/resampling/#resampling.Closed.RIGHT","title":"RIGHT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RIGHT = 1\n</code></pre> <p>Left-open, right-closed intervals: <code>(start, end]</code></p>"},{"location":"reference/resampling/#resampling.Closed-functions","title":"Functions","text":""},{"location":"reference/resampling/#resampling.Closed.members","title":"members  <code>staticmethod</code>","text":"<pre><code>members() -&gt; list[tuple[str, int]]\n</code></pre> <p>Returns a list of all members of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef members() -&gt; list[tuple[str, int]]:\n    \"\"\"Returns a list of all members of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Closed.values","title":"values  <code>staticmethod</code>","text":"<pre><code>values() -&gt; list[int]\n</code></pre> <p>Returns a list of all values of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef values() -&gt; list[int]:\n    \"\"\"Returns a list of all values of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Label","title":"resampling.Label","text":"<p>               Bases: <code>Enum</code></p> <p>Controls which edge of an interval is used as the output timestamp.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@unique\nclass Label(Enum):\n    \"\"\"Controls which edge of an interval is used as the output timestamp.\"\"\"\n\n    LEFT = 0\n    \"\"\"Use the interval start as the output timestamp\"\"\"\n    RIGHT = 1\n    \"\"\"Use the interval end as the output timestamp\"\"\"\n\n    @staticmethod\n    def values() -&gt; list[int]:\n        \"\"\"Returns a list of all values of the enum.\"\"\"\n\n    @staticmethod\n    def members() -&gt; list[tuple[str, int]]:\n        \"\"\"Returns a list of all members of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Label-attributes","title":"Attributes","text":""},{"location":"reference/resampling/#resampling.Label.LEFT","title":"LEFT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LEFT = 0\n</code></pre> <p>Use the interval start as the output timestamp</p>"},{"location":"reference/resampling/#resampling.Label.RIGHT","title":"RIGHT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>RIGHT = 1\n</code></pre> <p>Use the interval end as the output timestamp</p>"},{"location":"reference/resampling/#resampling.Label-functions","title":"Functions","text":""},{"location":"reference/resampling/#resampling.Label.members","title":"members  <code>staticmethod</code>","text":"<pre><code>members() -&gt; list[tuple[str, int]]\n</code></pre> <p>Returns a list of all members of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef members() -&gt; list[tuple[str, int]]:\n    \"\"\"Returns a list of all members of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Label.values","title":"values  <code>staticmethod</code>","text":"<pre><code>values() -&gt; list[int]\n</code></pre> <p>Returns a list of all values of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef values() -&gt; list[int]:\n    \"\"\"Returns a list of all values of the enum.\"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Resampler","title":"resampling.Resampler","text":"<p>The Resampler class is used to resample a time series of samples.</p> <p>It stores the samples in a buffer and resamples the samples in the buffer when the resample method is called. A resampler can be configured with a resampling function and a resampling interval.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>class Resampler:\n    \"\"\"\n    The Resampler class is used to resample a time series of samples.\n\n    It stores the samples in a buffer and resamples the samples in the buffer when the\n    resample method is called.\n    A resampler can be configured with a resampling function and a resampling interval.\n    \"\"\"\n\n    def __init__(\n        self,\n        interval: timedelta,\n        resampling_function: ResamplingFunction,\n        *,\n        max_age_in_intervals: int,\n        start: datetime,\n        closed: Closed,\n        label: Label,\n    ):\n        \"\"\"\n        Initializes a new Resampler object.\n\n        Args:\n            interval: The resampling interval.\n            resampling_function: The resampling function.\n            max_age_in_intervals: The maximum age of a sample in intervals.\n            start: The start time of the resampling.\n            closed: Which interval edge is closed for sample membership. Use\n                `\"left\"` for left-closed, right-open intervals `[start, end)`\n                and `\"right\"` for right-closed, left-open intervals\n                `(start, end]`.\n            label: Which interval edge to use for output timestamps. Use\n                `\"left\"` for the left bin edge and `\"right\"` for the right\n                bin edge.\n        \"\"\"\n\n    def push_sample(self, *, timestamp: datetime, value: Optional[float]) -&gt; None:\n        \"\"\"\n        Pushes a new sample into the resampler buffer.\n\n        Args:\n            timestamp: The timestamp of the sample.\n            value: The value of the sample.\n        \"\"\"\n\n    def resample(\n        self, end: datetime | None = None\n    ) -&gt; list[tuple[datetime, Optional[float]]]:\n        \"\"\"\n        Resamples the samples in the buffer until the given end time.\n\n        Args:\n            end: The end time of the resampling. If `None` the samples in the buffer will be\n                resampled until the current date/time.\n\n        Returns:\n            A list of tuples with the resampled samples.\n        \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Resampler-functions","title":"Functions","text":""},{"location":"reference/resampling/#resampling.Resampler.__init__","title":"__init__","text":"<pre><code>__init__(\n    interval: timedelta,\n    resampling_function: ResamplingFunction,\n    *,\n    max_age_in_intervals: int,\n    start: datetime,\n    closed: Closed,\n    label: Label\n)\n</code></pre> <p>Initializes a new Resampler object.</p> PARAMETER DESCRIPTION <code>interval</code> <p>The resampling interval.</p> <p> TYPE: <code>timedelta</code> </p> <code>resampling_function</code> <p>The resampling function.</p> <p> TYPE: <code>ResamplingFunction</code> </p> <code>max_age_in_intervals</code> <p>The maximum age of a sample in intervals.</p> <p> TYPE: <code>int</code> </p> <code>start</code> <p>The start time of the resampling.</p> <p> TYPE: <code>datetime</code> </p> <code>closed</code> <p>Which interval edge is closed for sample membership. Use <code>\"left\"</code> for left-closed, right-open intervals <code>[start, end)</code> and <code>\"right\"</code> for right-closed, left-open intervals <code>(start, end]</code>.</p> <p> TYPE: <code>Closed</code> </p> <code>label</code> <p>Which interval edge to use for output timestamps. Use <code>\"left\"</code> for the left bin edge and <code>\"right\"</code> for the right bin edge.</p> <p> TYPE: <code>Label</code> </p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>def __init__(\n    self,\n    interval: timedelta,\n    resampling_function: ResamplingFunction,\n    *,\n    max_age_in_intervals: int,\n    start: datetime,\n    closed: Closed,\n    label: Label,\n):\n    \"\"\"\n    Initializes a new Resampler object.\n\n    Args:\n        interval: The resampling interval.\n        resampling_function: The resampling function.\n        max_age_in_intervals: The maximum age of a sample in intervals.\n        start: The start time of the resampling.\n        closed: Which interval edge is closed for sample membership. Use\n            `\"left\"` for left-closed, right-open intervals `[start, end)`\n            and `\"right\"` for right-closed, left-open intervals\n            `(start, end]`.\n        label: Which interval edge to use for output timestamps. Use\n            `\"left\"` for the left bin edge and `\"right\"` for the right\n            bin edge.\n    \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Resampler.push_sample","title":"push_sample","text":"<pre><code>push_sample(\n    *, timestamp: datetime, value: Optional[float]\n) -&gt; None\n</code></pre> <p>Pushes a new sample into the resampler buffer.</p> PARAMETER DESCRIPTION <code>timestamp</code> <p>The timestamp of the sample.</p> <p> TYPE: <code>datetime</code> </p> <code>value</code> <p>The value of the sample.</p> <p> TYPE: <code>Optional[float]</code> </p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>def push_sample(self, *, timestamp: datetime, value: Optional[float]) -&gt; None:\n    \"\"\"\n    Pushes a new sample into the resampler buffer.\n\n    Args:\n        timestamp: The timestamp of the sample.\n        value: The value of the sample.\n    \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.Resampler.resample","title":"resample","text":"<pre><code>resample(\n    end: datetime | None = None,\n) -&gt; list[tuple[datetime, Optional[float]]]\n</code></pre> <p>Resamples the samples in the buffer until the given end time.</p> PARAMETER DESCRIPTION <code>end</code> <p>The end time of the resampling. If <code>None</code> the samples in the buffer will be resampled until the current date/time.</p> <p> TYPE: <code>datetime | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>list[tuple[datetime, Optional[float]]]</code> <p>A list of tuples with the resampled samples.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>def resample(\n    self, end: datetime | None = None\n) -&gt; list[tuple[datetime, Optional[float]]]:\n    \"\"\"\n    Resamples the samples in the buffer until the given end time.\n\n    Args:\n        end: The end time of the resampling. If `None` the samples in the buffer will be\n            resampled until the current date/time.\n\n    Returns:\n        A list of tuples with the resampled samples.\n    \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.ResamplingFunction","title":"resampling.ResamplingFunction","text":"<p>               Bases: <code>Enum</code></p> <p>The ResamplingFunction enum represents the different resampling functions that can be used to resample a time series.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@unique\nclass ResamplingFunction(Enum):\n    \"\"\"\n    The ResamplingFunction enum represents the different resampling functions\n    that can be used to resample a time series.\n    \"\"\"\n\n    AVERAGE = 0\n    \"\"\"Calculates the average of all samples in the time step (ignoring None values)\"\"\"\n    SUM = 1\n    \"\"\"Calculates the sum of all samples in the time step (ignoring None values)\"\"\"\n    MAX = 2\n    \"\"\"Calculates the maximum of all samples in the time step\"\"\"\n    MIN = 3\n    \"\"\"Calculates the minimum of all samples in the time step\"\"\"\n    LAST = 4\n    \"\"\"Returns the last sample in the time step\"\"\"\n    COUNT = 5\n    \"\"\"Counts the number of samples in the time step\"\"\"\n    FIRST = 6\n    \"\"\"Returns the first sample in the time step\"\"\"\n    COALESCE = 7\n    \"\"\"Returns the first non-None sample in the time step\"\"\"\n\n    @staticmethod\n    def values() -&gt; list[int]:\n        \"\"\"\n        Returns a list of all values of the enum.\n\n        Returns:\n            A list of all values of the enum.\n        \"\"\"\n\n    @staticmethod\n    def members() -&gt; list[tuple[str, int]]:\n        \"\"\"\n        Returns a list of all members of the enum.\n\n        Returns:\n            A list of all members of the enum.\n        \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.ResamplingFunction-attributes","title":"Attributes","text":""},{"location":"reference/resampling/#resampling.ResamplingFunction.AVERAGE","title":"AVERAGE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>AVERAGE = 0\n</code></pre> <p>Calculates the average of all samples in the time step (ignoring None values)</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.COALESCE","title":"COALESCE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>COALESCE = 7\n</code></pre> <p>Returns the first non-None sample in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.COUNT","title":"COUNT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>COUNT = 5\n</code></pre> <p>Counts the number of samples in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.FIRST","title":"FIRST  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FIRST = 6\n</code></pre> <p>Returns the first sample in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.LAST","title":"LAST  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>LAST = 4\n</code></pre> <p>Returns the last sample in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.MAX","title":"MAX  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MAX = 2\n</code></pre> <p>Calculates the maximum of all samples in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.MIN","title":"MIN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>MIN = 3\n</code></pre> <p>Calculates the minimum of all samples in the time step</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction.SUM","title":"SUM  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SUM = 1\n</code></pre> <p>Calculates the sum of all samples in the time step (ignoring None values)</p>"},{"location":"reference/resampling/#resampling.ResamplingFunction-functions","title":"Functions","text":""},{"location":"reference/resampling/#resampling.ResamplingFunction.members","title":"members  <code>staticmethod</code>","text":"<pre><code>members() -&gt; list[tuple[str, int]]\n</code></pre> <p>Returns a list of all members of the enum.</p> RETURNS DESCRIPTION <code>list[tuple[str, int]]</code> <p>A list of all members of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef members() -&gt; list[tuple[str, int]]:\n    \"\"\"\n    Returns a list of all members of the enum.\n\n    Returns:\n        A list of all members of the enum.\n    \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling.ResamplingFunction.values","title":"values  <code>staticmethod</code>","text":"<pre><code>values() -&gt; list[int]\n</code></pre> <p>Returns a list of all values of the enum.</p> RETURNS DESCRIPTION <code>list[int]</code> <p>A list of all values of the enum.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>@staticmethod\ndef values() -&gt; list[int]:\n    \"\"\"\n    Returns a list of all values of the enum.\n\n    Returns:\n        A list of all values of the enum.\n    \"\"\"\n</code></pre>"},{"location":"reference/resampling/#resampling-functions","title":"Functions","text":""},{"location":"reference/resampling/#resampling.resample","title":"resampling.resample","text":"<pre><code>resample(\n    data: Sequence[tuple[datetime, Optional[float]]],\n    interval: timedelta,\n    method: ResamplingFunction,\n    *,\n    closed: Closed,\n    label: Label\n) -&gt; list[tuple[datetime, Optional[float]]]\n</code></pre> <p>Resamples a list of timestamp/value pairs in a single call.</p> <p>This is a convenience function for one-shot resampling without needing to manage a <code>Resampler</code> instance.</p> PARAMETER DESCRIPTION <code>data</code> <p>A list of (timestamp, value) tuples to resample. Must be sorted by timestamp.</p> <p> TYPE: <code>Sequence[tuple[datetime, Optional[float]]]</code> </p> <code>interval</code> <p>The resampling interval.</p> <p> TYPE: <code>timedelta</code> </p> <code>method</code> <p>The resampling function to use for aggregating values within each interval.</p> <p> TYPE: <code>ResamplingFunction</code> </p> <code>closed</code> <p>Which interval edge is closed for sample membership. Use <code>\"left\"</code> for left-closed, right-open intervals <code>[start, end)</code> and <code>\"right\"</code> for right-closed, left-open intervals <code>(start, end]</code>.</p> <p> TYPE: <code>Closed</code> </p> <code>label</code> <p>Which interval edge to use for output timestamps. Use <code>\"left\"</code> for the left bin edge and <code>\"right\"</code> for the right bin edge.</p> <p> TYPE: <code>Label</code> </p> RETURNS DESCRIPTION <code>list[tuple[datetime, Optional[float]]]</code> <p>A list of (timestamp, value) tuples representing the resampled data.</p> Source code in <code>frequenz/resampling/_rust_backend.pyi</code> <pre><code>def resample(\n    data: Sequence[tuple[datetime, Optional[float]]],\n    interval: timedelta,\n    method: ResamplingFunction,\n    *,\n    closed: Closed,\n    label: Label,\n) -&gt; list[tuple[datetime, Optional[float]]]:\n    \"\"\"\n    Resamples a list of timestamp/value pairs in a single call.\n\n    This is a convenience function for one-shot resampling without needing to\n    manage a `Resampler` instance.\n\n    Args:\n        data: A list of (timestamp, value) tuples to resample. Must be sorted by timestamp.\n        interval: The resampling interval.\n        method: The resampling function to use for aggregating values within each interval.\n        closed: Which interval edge is closed for sample membership. Use\n            `\"left\"` for left-closed, right-open intervals `[start, end)`\n            and `\"right\"` for right-closed, left-open intervals `(start, end]`.\n        label: Which interval edge to use for output timestamps. Use `\"left\"`\n            for the left bin edge and `\"right\"` for the right bin edge.\n\n    Returns:\n        A list of (timestamp, value) tuples representing the resampled data.\n    \"\"\"\n</code></pre>"}]}